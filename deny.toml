[advisories]
version = 2
ignore = [
    "RUSTSEC-2024-0436",  # paste unmaintained — transitive via ratatui, no action available
    "RUSTSEC-2024-0384",  # instant unmaintained — transitive via notify, no action available
]

[licenses]
version = 2
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Zlib",
    "CC0-1.0",
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
